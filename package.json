{
  "name": "syndicate",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,blue \"npm run -w server dev\" \"wait-on tcp:localhost:8000 && npm run -w client dev\"",
    "server:dev": "npm run -w server dev",
    "client:dev": "npm run -w client dev",
    "client:dev:wait": "wait-on tcp:localhost:8000 && npm run -w client dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^8.0.0"
  }
}
